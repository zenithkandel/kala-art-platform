<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title><%= title %> · कला Admin</title>
		<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css" />
		<link rel="stylesheet" href="/public/css/base.css" />
		<style>
			.login-wrap{max-width:420px;margin:10vh auto;padding:2rem;border:1px solid var(--border,#333);border-radius:12px;background:var(--surface,#111)}
			.login-wrap h1{margin:0 0 1rem;display:flex;gap:.5rem;align-items:center}
			.field{display:flex;flex-direction:column;gap:.5rem;margin:.75rem 0}
			.field input{padding:.75rem;border-radius:8px;border:1px solid var(--border,#333);background:transparent;color:inherit}
			.btn{width:100%;padding:.9rem;border-radius:10px;border:1px solid var(--border,#333);background:#7c4dff;color:#fff}
			.alert{margin:.5rem 0;padding:.75rem;border-radius:8px}
			.alert--error{background:#2a0d0d;color:#f99}
		</style>
	</head>
	<body>
		<main class="login-wrap">
			<h1><i class="fa-solid fa-shield-halved"></i> Admin Login</h1>
			<% if (error) { %>
				<div class="alert alert--error"><i class="fa-solid fa-circle-exclamation"></i> <%= error %></div>
			<% } %>
			<form method="post" action="/admin/login">
				<div class="field">
					<label for="username"><i class="fa-solid fa-user"></i> Username</label>
					<input id="username" name="username" autocomplete="username" required />
				</div>
				<div class="field">
					<label for="password"><i class="fa-solid fa-key"></i> Password</label>
					<input id="password" name="password" type="password" autocomplete="current-password" required />
				</div>
				<button class="btn" type="submit"><i class="fa-solid fa-right-to-bracket"></i> Sign in</button>
			</form>
		</main>
	</body>
	</html>

